<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.space.mapper.MsgBoardMapper">
    <insert id="insert">
        insert into msg_board (id, wrid, content, time, floor)
        values (#{id},#{wrid},#{content},#{time},#{floor});
    </insert>
</mapper>